# Project Report (Meeting Notes Draft)

Date: 2026-05-06  
Branch reviewed: `feature-lon`

## 1) Goal of this report
Provide a fast, shared view of what is done, what is pending, and what decisions we should make together before merge.

## 2) Current status snapshot
- Core project app scaffold is in place and runs locally.
- CRUD-style form UI has been added across entity pages.
- DB connector is configured locally (credentials handled outside tracked git file).
- README currently aligned to `main` until team decides final merge content.

## 3) What was completed
- Step 3 DML file created (`week05/group16_DML.sql`).
- Node + Handlebars structure created for draft app.
- Routes/pages present for:
  - Home
  - Patients
  - Appointment Types
  - Providers
  - Clinics
  - Appointments
  - Provider Locations
- Forms are added to support create/update/delete in the UI.

## 4) What is intentionally NOT final yet
- Final team-reviewed README updates for Step 3.
- Any optional enhancements beyond assignment minimum.
- Final merge to `main`.

## 5) Items to review together in meeting
1. Scope check: Is every implemented page required for this step?
2. UI check: Keep all CRUD forms as-is or trim to required minimum?
3. Data layer check: Confirm no sensitive credentials are tracked.
4. Deployment check: Confirm the one production URL/port for submission.
5. Submission packaging check: exact files and naming.

## 6) Safe plan
- No new features until both teammates agree.
- Merge only what is required first.
- Save optional polish for later.

## 7) Action checklist
- [ ] Review changed files together.
- [ ] Confirm required vs optional items.
- [ ] Approve merge list.
- [ ] Merge feature-lon into main.
- [ ] Update README after merge.
- [ ] Confirm final URL and submission items.

## 8) Quick decision box
- Keep current scope: Yes / No
- Remove anything before merge: Yes / No (list it)
- Merge today: Yes / No
- Task owners:
  - URL check:
  - ZIP package:
  - Ed post:
  - Canvas link:
